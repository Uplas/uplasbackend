STRIPE_PUBLISHABLE_KEY=pk_test_yourpublishablekey # Your Stripe publishable key (test mode)
STRIPE_SECRET_KEY=sk_test_yoursecretkey         # Your Stripe secret key (test mode)
STRIPE_WEBHOOK_SECRET_PAYMENTS=whsec_yourwebhooksecret # Your Stripe webhook signing secret


# .env.example
# Copy this file to .env for local development and fill in the values.
# NEVER commit the actual .env file.
# In production (GCP), set these variables directly in the environment.

# --- Core Django Settings ---
DJANGO_SECRET_KEY="your-strong-and-secret-key-here"
DJANGO_DEBUG="True" # Set to "False" in production
DJANGO_ALLOWED_HOSTS="localhost,127.0.0.1,your-production-domain.com"
DJANGO_CSRF_TRUSTED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000,https://your-frontend-domain.com"

# --- Database Settings (Example for Local/Proxy) ---
DB_ENGINE="django.db.backends.mysql"
DB_NAME="uplas_dev_db"
DB_USER="root"
DB_PASSWORD="your-local-db-password"
DB_HOST="127.0.0.1"
DB_PORT="3306"
# --- Database Settings (Example for Cloud SQL via Socket - Set HOST in settings.py or use this) ---
# INSTANCE_CONNECTION_NAME="your-gcp-project:your-region:your-instance" # Use this in GCP

# --- Localization ---
DJANGO_LANGUAGE_CODE="en-us"
DJANGO_TIME_ZONE="Africa/Nairobi"

# --- Google Cloud Storage (Production) ---
GS_BUCKET_NAME="your-gcp-storage-bucket-name"

# --- Django REST Framework & JWT ---
DRF_PAGE_SIZE="10"
JWT_ACCESS_TOKEN_LIFETIME_MINUTES="60"
JWT_REFRESH_TOKEN_LIFETIME_DAYS="7"

# --- CORS ---
DJANGO_CORS_ALLOWED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000,https://your-frontend-domain.com"

# --- Email (Production Example - Use Console Backend for Dev) ---
# DJANGO_EMAIL_BACKEND="django.core.mail.backends.smtp.EmailBackend"
# DJANGO_EMAIL_HOST="smtp.your-email-provider.com"
# DJANGO_EMAIL_PORT="587"
# DJANGO_EMAIL_USE_TLS="True"
# DJANGO_EMAIL_HOST_USER="your-email-user"
# DJANGO_EMAIL_HOST_PASSWORD="your-email-password"
# DJANGO_DEFAULT_FROM_EMAIL="UPLAS Platform <noreply@uplas.me>"

# --- Custom App Settings ---
WHATSAPP_CODE_EXPIRY_MINUTES="10"

# --- Stripe Settings ---
STRIPE_PUBLISHABLE_KEY="pk_test_your_stripe_publishable_key"
STRIPE_SECRET_KEY="sk_test_your_stripe_secret_key"
STRIPE_WEBHOOK_SECRET="whsec_your_stripe_webhook_secret"

# --- AI Agent Service URLs ---
AI_NLP_TUTOR_SERVICE_URL="http://localhost:8001/api/v1/ai-tutor" # Update with actual deployed URL
AI_TTS_SERVICE_URL="http://localhost:8002/api/v1/tts" # Update with actual deployed URL
AI_TTV_SERVICE_URL="http://localhost:8003/api/v1/ttv" # Update with actual deployed URL
AI_PROJECT_GENERATOR_SERVICE_URL="http://localhost:8004/api/v1/project-generator" # Update with actual deployed URL
AI_PROJECT_ASSESSMENT_SERVICE_URL="http://localhost:8004/api/v1/project-assessment" # Update with actual deployed URL
AI_SERVICE_API_KEY="your-secret-key-to-secure-your-ai-services" # Optional, but recommended
